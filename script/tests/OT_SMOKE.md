# OT Smoke Tests - Acceptance Checklist

Run after EVERY step:

- [ ] **A1**) App opens once (single "Shell started").
- [ ] **A2**) Import video -> first frame visible immediately.
- [ ] **A3**) ROI editable manually.
- [ ] **A4**) Preview Gate PASS works.
- [ ] **A5**) RUN completes (short range ok) and UI stays responsive (window draggable).
- [ ] **A6**) v2 outputs exist in `ot_v2_shadow` (`ot_summary.json`, `qc.json`, `camera_meta.json`, `trajectory.csv`, `run_manifest.json`).
